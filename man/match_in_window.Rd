% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datawrangling_util.R
\name{match_in_window}
\alias{match_in_window}
\title{Match extreme values of a secondary variable within a time window}
\usage{
match_in_window(
  df_x,
  df_y,
  id,
  x_value,
  x_time,
  y_value,
  y_time,
  w,
  side = c("both", "prior", "post"),
  extreme = c("max", "min")
)
}
\arguments{
\item{df_x}{A data frame containing the primary measurements.}

\item{df_y}{A data frame containing the secondary measurements to match against.}

\item{id}{Column name identifying subjects (must be present in both data frames).}

\item{x_value}{Column name of the variable of interest in \code{df_x}.}

\item{x_time}{Column name of the numeric test time for the variable in \code{df_x}.}

\item{y_value}{Column name of the variable to attach from \code{df_y}.}

\item{y_time}{Column name of the numeric test time for the variable in \code{df_y}.}

\item{w}{Numeric window width. Candidate \code{y_time} values must be within this
window relative to \code{x_time}.}

\item{side}{Character, one of \code{"prior"}, \code{"post"}, or \code{"both"}. Controls
whether the time window is before, after, or either side of \code{x_time}.
\itemize{
\item \code{"prior"}: \verb{y_time ∈ [x_time - w, x_time)}
\item \code{"post"}:  \verb{y_time ∈ (x_time, x_time + w]}
\item \code{"both"}:  \verb{y_time ∈ [x_time - w, x_time + w]}
}

[x_time - w, x_time)`
\itemize{
\item \code{"post"}:  `y_time ∈ (x_time, x_time + w]: R:x_time\%20-\%20w,\%20x_time)\%60\%0A\%20\%20-\%20\%60\%22post\%22\%60:\%20\%20\%60y_time\%20\%E2\%88\%88\%20(x_time,\%20x_time\%20+\%20w
[x_time - w, x_time + w]: R:x_time\%20-\%20w,\%20x_time\%20+\%20w
}}

\item{extreme}{Character, \code{"max"} or \code{"min"}. Whether to select the maximum
or minimum \code{y_value} within the window.}
}
\value{
A tibble consisting of all columns of \code{df_x} plus:
\itemize{
\item \code{matched_y}: the selected \code{y_value} (or \code{NA} if no match),
\item \code{matched_y_time}: the corresponding \code{y_time}.
}
}
\description{
For each row in a primary data frame (\code{df_x}), this function looks up records
in a secondary data frame (\code{df_y}) for the same subject and finds the single
extreme (maximum or minimum) value of a target variable (\code{y_value})
whose test time is within a specified window of the primary variable’s test time.
}
\details{
If multiple candidate records share the same extreme value, ties are broken
deterministically: the record closest in time to \code{x_time} is chosen; if still
tied, the earliest \code{y_time} is selected. If no records fall within the window,
the merged columns will contain \code{NA}.
}
\examples{
\dontshow{if (requireNamespace("tibble", quietly = TRUE)) withAutoprint(\{ # examplesIf}
df_x <- tibble::tibble(
  id  = c(1, 1, 2),
  x   = c(10, 20, 30),
  t_x = c(100, 200, 150)
)

df_y <- tibble::tibble(
  id  = c(1, 1, 1, 2, 2),
  y   = c(5, 7, 7, 9, 8),
  t_y = c(95, 85, 110, 140, 180)
)

match_in_window(
  df_x, df_y,
  id = id,
  x_value = x, x_time = t_x,
  y_value = y, y_time = t_y,
  w = 20,
  side = "both",
  extreme = "max"
)
\dontshow{\}) # examplesIf}
}
